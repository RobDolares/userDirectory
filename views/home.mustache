<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Robo Directory</title>
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body>
    <header>

    </header>
    <main>
      <div class="dirContainer">
        {{#users}}
        <div class="userContainer">
          <div class="headshot">
            <a href="/bio/{{username}}"><img src="{{avatar}}" alt=""></a>
          </div>
          <h2 class="userName">{{name}}</h2>
          <p>{{job}}</p>
          <p>{{company}}</p>
        </div>
        {{/users}}
      </div>
    </main>
  </body>
</html>
